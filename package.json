{
  "name": "brava-dubai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check netlify/functions/api.mjs && node --check netlify/functions/_mail.mjs && node --check netlify/functions/inmueble.mjs && node --check netlify/functions/investor-expiry-reminders.mjs && npm test"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/database": "^1.0.0"
  }
}
